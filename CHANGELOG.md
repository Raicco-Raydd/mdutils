# Changelog

## v2.3.1 — 2026-08-08

### 🐛 修复

- **新手引导 · 移动端目录步骤**：手机端目录树收在悬浮球抽屉里，引导到「文档结构」步骤时自动展开抽屉再高亮，结束引导自动收起；修复手机端该步骤高亮指向不可见元素的问题

---

## v2.3.0 — 2026-08-08

### ✨ 新增

- **Vim / Emacs 键位编辑模式**：编辑面板新增「键位」切换（普通 / Vim / Emacs）
  - Vim：j/k/h/l 移动、i/a/o/O 插入、dd 删行、x 删字符、yy 复制行、p 粘贴、gg/G 跳文档首尾、0/$ 行首尾、u 撤销（独立 20 层栈）、Esc 切换模式
  - Emacs：C-f/b/n/p 移动、C-a/e 行首尾、C-k 删至行尾、C-d 删字符、C-y 粘贴、C-z 与 C-/ 撤销（C-s 保存复用全局快捷键）
  - 编辑器边框随模式变色：Vim 青 / Vim-插入 绿 / Emacs 黄，模式状态一目了然

### 🐛 修复

- **新手引导移动端适配**：≤768px 时引导卡片改为底部横条，顶部指示箭头实时指向高亮元素，解决手机端「按钮和高亮不在一个地方、看不到高亮在哪」的问题；触控按钮加大点击区

---

## v2.2.0 — 2026-08-07

### 🆕 新增

- **日间 / 夜间 / 跟随系统主题切换**：顶栏新增主题按钮（🖥 跟随系统 → ☀️ 日间 → 🌙 夜间 三态循环），localStorage 持久化 + 预载防闪烁（FOUC），自动跟随系统深浅色（prefers-color-scheme）
- 全站配色重构为 CSS 变量体系（19 个 token），清理全部硬编码色值

### 🎨 界面优化

- 日间主题：浅色面板 + 深青绿强调色，代码块 / 表格 / 引导蒙版 / 遮罩全部适配
- 夜间主题：保持原暗色风格不变

---

## v2.1.0 — 2026-08-06

### 🆕 新增

- **目录树引导标识**：树形连接线（父块主线 + 子项短横）、每项 H1-H6 级别徽标、滚动跟随高亮当前阅读区块
- **「＋ 添加区块」**：预览区块分割线处添加新区块，右侧面板新增“添加”模式（首行写标题，级别自动继承锚点）
- **历史记录**：最近打开的文档（去重、显示最近 5 个、localStorage 持久化）
- **蒙版式引导**：首次访问自动播放的 12 步聚光灯教程（快速上手按钮可重播）
- **状态栏升级**：文件名 + 字符/行/标题统计高亮框，瞬时消息独立显示
- `editor.insert_section_after(text, anchor, new_heading, content)`：在指定区块之后插入新区块

### 🎨 界面优化

- 顶栏三栏布局：品牌+表单 / 文档信息居中 / 历史+快速上手居右
- 预览整体左移（靠左锚定），🖊 编辑入口改为紧跟标题文字
- 编辑面板从弹窗改为右侧常驻（上编辑器 / 下实时预览，带“预览”分隔行）

### 🔧 修复

- 修复引导卡片“下一步”按钮出框（进度圆点 flex 收缩）
- 卡片切换位置改为平滑过渡

### 🐛 内部

- Web UI 自动化测试 45/45 全过（读取/替换/插入/删除/添加/元信息/异常路径）

---

## v2.0.0 — 2026-08-06

### 🆕 重大更新：Web UI（webui/）

- **预览界面**：本地 FastAPI 服务器 + 深色双栏界面（左目录树 / 右渲染预览），marked + highlight.js 离线渲染
- **结构化编辑**：按区块编辑（替换/删除/标题后插入），全部走 mdutils 引擎
- **目录浏览**：文件浏览器弹窗（子目录/ .md 文件导航）
- **YAML 元信息**：更新/创建 frontmatter
- **撤销**：栈式 20 步，可回滚任意修改
- **插入符号菜单**：18 个 Markdown 符号一键插入（标题/加粗/目录生成等）

### 🔌 新增接口

- `GET /api/file`、`GET /api/dir`、`GET /api/section`
- `POST /api/section/edit|delete|insert`、`POST /api/frontmatter`、`POST /api/file`

### 📦 运行

```bash
cd webui
pip install fastapi uvicorn
py -m uvicorn app:app --host 127.0.0.1 --port 8765
# 浏览器打开 http://127.0.0.1:8765
```

---

## v1.2.0 — 2026-07-12

### 🆕 新增

- **错误提示系统**：所有 CLI 命令新增带 `❌` / `⚠️` 前缀的错误/警告信息，输出到 stderr
- **FAQ 常见问题**：README 新增 FAQ 章节，覆盖管道中文、标题匹配、表格校验等常见坑
- **30 秒快速上手**：README 顶部新增极简入门示例

### 🔧 优化

- **命令校验增强**：
  - `extract-section`：标题未找到时输出 `⚠️` 警告（不再静默返回空）
  - `replace-section` / `delete-section`：标题未找到时明确报错并退出
  - `insert-after`：捕获 `ValueError` 并显示可读错误
  - `generate table`：行列数不一致时输出 `⚠️` 警告
  - `generate link / image`：参数不足时显示用法提示
  - `read`：文件不存在/无权限时明确报错
  - `find`：目录不存在时明确报错
  - `write`：写入失败时显示具体原因
  - 无输入时（无管道、无参数）给出明确指引
- **README 结构改进**：CLI 版 → Python API 版的文档排列更清晰

### 🐛 修复

- **README 表头识别**：修正 `extract_tables()` 中因首列空导致的表头列数异常

---

## v1.1.0 — 2026-07-12

### 🆕 新增

- **CLI 使用文档**：README 新增完整的命令行参考（13 个命令+示例用法），不再仅限 Python API 文档
- **Windows GBK 编码回退**：`read_md()` 读取文件时如 UTF-8 解码失败，自动尝试 GBK 编码，兼容 Windows 记事本保存的 .md 文件

### 🔧 优化

- **stdout UTF-8 自动适配**：CLI 启动时检测 stdout 编码，若非 UTF-8 则自动切换，确保 emoji 和 CJK 字符正常输出
- **README 结构调整**：CLI 文档放在「功能模块」之后、「安装」之前，阅读流程更自然

### 🐛 修复

- **`replace_section()` 边界情况**：修复当目标标题是文档末尾标题时，替换后丢失尾部换行的问题
- **文档结构树兼容性**：`get_structure()` 对无标题文档返回空列表而非报错

---

## v0.1.0 — 2026-07-11

- 首次发布
- 核心功能：Markdown 解析（标题/区块/代码块/表格）、编辑（替换/删除/插入）、生成（表格/列表/代码块/链接/图片）、文件工具
